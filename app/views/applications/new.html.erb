<div class="container-fluid">
	<h2 align="center">Register a New Application</h2>
	<div class="form-wide" align="center">
		<div class="user-form">
			<%= simple_form_for [@user, @user.applications.new] do |f| %>
				<%= f.error_notification %>
				<%= f.input :name %>
				<%= f.input :url %>
				<%= f.button :submit %>
			<% end %>
		</div>
	</div>
</div>